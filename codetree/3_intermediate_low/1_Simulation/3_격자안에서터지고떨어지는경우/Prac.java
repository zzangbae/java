import java.util.*;

/**
 * 격자 안에서 터지고 떨어지는 경우
 * 1. 특정 규칙에 의해 폭탄이 터지고
 * 2. 이후 중력이 작용하여 위에 떠있는 폭탄들이 아래로 떨어짐
 */
public class Prac {
    public static void main(String[] args) {
        // 1. temp 배열 만들기
        // 2. arr 배열을 아래서부터 올라가며 temp에 추가
        // 3. temp 값을 arr 값에 복사하기
    }    
}
